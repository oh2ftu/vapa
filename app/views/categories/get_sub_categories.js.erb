$('.sub_category_selection').empty();
$('.sub_category_selection').append( $('<option>Select a SubCategory</option>'));
<% @sub_categories.each do |sub_category| %>
  $('.sub_category_selection').append($('<option value="<%= sub_category.id %>"><%= sub_category.name %></option>'));
<% end %>
